<!--include(~/dotfiles/AGENTS.md)-->

<!--
This file is local to the current project. Add project-specific
agent configuration below.
-->

## Development Rules

- Do NOT run `wails build`, `wails dev`, or any Wails compilation commands
- Only run `go test` for testing
- Create `*_test.go` files for unit tests
- I (the user) will handle Wails compilation manually
